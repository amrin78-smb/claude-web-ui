# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
web/dist/

# Packaging output — staged trees and installers, rebuilt by scripts/package.js
dist-pkg/

# Local runtime state — machine-specific, never shared
config.json
sessions.json
server.pid
.claude-web-images/
.claude-web-files/

# Old packaged artifact, not source
claude-web-ui.zip

# Playwright MCP scratch output (snapshots/console logs from driving the UI)
.playwright-mcp/

# Claude Code local (per-user) settings — settings.json itself (if added) is shared
.claude/settings.local.json

# Editor/OS junk
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
